const { add, subtract, multiply } = require('../ex01/mathOperations.js');

const addOperation = add(20, 10);
console.log(addOperation);

const subtractOperation = subtract(20, 10);
console.log(subtractOperation);

const multiplyOperation = multiply(20, 10);
console.log(multiplyOperation);

